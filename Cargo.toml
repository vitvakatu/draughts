[package]
name = "draughts"
version = "0.1.0"
authors = ["Ilya Bogdanov <fumlead@gmail.com>"]

[dependencies]
three = { git = "https://github.com/three-rs/three/" }
euler = { version = "0.2", features = ["mint"] }
ndarray = "0.10"
